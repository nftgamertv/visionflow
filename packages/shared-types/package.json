{
  "name": "@visionflow/shared-types",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate-types": "node scripts/generate-types.js",
    "lint": "echo 'No linting for shared-types'",
    "build": "echo 'No build required for shared-types'",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@apidevtools/openapi-schemas": "^2.1.0",
    "openapi-zod-client": "^1.13.2",
    "typescript": "^5.3.3"
  }
}
